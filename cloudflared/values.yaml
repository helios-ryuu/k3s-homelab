# =================================================================
# Cloudflare Tunnel — kết nối K3s services ra internet (k3s)
# =================================================================
# Zero-downtime: 2 replicas, prefer master nodes, fallback to workers
# Nếu tất cả masters offline → tự chạy trên bất kỳ worker nào
# Anti-affinity: 2 pod luôn trên 2 node khác nhau
# =================================================================
# Token: kubectl get secret infra-secrets -n cloudflared
# =================================================================

replicas: 2
